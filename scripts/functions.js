/*
Can only be used on pages over https can implement later!
function copyText(){
        const email = document.getElementById("email").innerHTML;
    
    navigator.clipboard.writeText(email)
    console.alert("Copied email: " + email);
}
*/